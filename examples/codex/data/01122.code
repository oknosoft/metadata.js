// Если раскомментировать следующий и закомментировать текущий вариант, форма откроется в новом режиме
// Поддержаны синонимы. $p.cat.Контрагенты.form_selection($p.iface.docs, {}) эквивалентно Справочники.Контрагенты.ФормаВыбора(ОбластьКонтента, {})

Документы.ЗаказПокупателя.Найти().ФормаОбъекта(ОбластьКонтента, {Приклеить: Истина});// Форма первого попавшегося заказа приклеена к основному окну

//Документы.ЗаказПокупателя.Найти({number_doc: "ТД00-000004"}).ФормаОбъекта();      // Форма заказа №ТД00-000004 в отдельном диалоге