<!---->
// Если раскомментировать следующий и закомментировать текущий вариант, форма откроется в новом режиме
// Поддержаны синонимы. $p.cat.Контрагенты.form_selection($p.iface.docs) эквивалентно Справочники.Контрагенты.ФормаВыбора(ОбластьКонтента)

Справочники.Контрагенты.ФормаВыбора(ОбластьКонтента);	// Форма списка контрагентов приклеена к основному окну

// Форма списка контрагентов в модальном диалоге с обработкой результата
//Справочники.Контрагенты.ФормаВыбора({on_select: function (sval) {Сообщить(sval.name);}});

//Справочники.Номенклатура.ФормаВыбора();				// Форма списка номенклатуры в модальном диалоге

//Документы.ЗаказПокупателя.ФормаВыбора(ОбластьКонтента);// Список заказов приклеен к основному окну