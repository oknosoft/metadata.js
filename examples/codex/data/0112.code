<!---->
// Если раскомментировать следующий и закомментировать текущий вариант, форма откроется в новом режиме
// Поддержаны синонимы. $p.cat.Контрагенты.form_selection($p.iface.docs, {}) эквивалентно Справочники.Контрагенты.ФормаВыбора(ОкноПриложения, {})

Справочники.Контрагенты.ФормаВыбора(ОкноПриложения, {});    // Форма списка контрагентов приклеена к основному окну

//Справочники.Контрагенты.ФормаВыбора({}, {});              // Форма списка контрагентов в модальном диалоге

//Справочники.Номенклатура.ФормаВыбора({}, {});             // Форма списка номенклатуры в модальном диалоге

//Документы.ЗаказПокупателя.ФормаВыбора(ОкноПриложения, {});// Список заказов приклеен к основному окну