<!---->
// Если раскомментировать следующий и закомментировать текущий вариант, форма откроется в новом режиме

// Форма списка контрагентов приклеена к основному окну
$p.cat.Контрагенты.form_selection($p.iface.docs, {});

// Форма списка контрагентов в модальном диалоге
//$p.cat.Контрагенты.form_selection({}, {});

// Форма списка номенклатуры в модальном диалоге
//$p.cat.Номенклатура.form_selection({}, {});