// Открываем форму первого попавшегося заказа
Документы.ЗаказПокупателя.Найти().ФормаОбъекта(ОбластьКонтента, {Приклеить: Истина})
	.then(function(Рез){
		// Выделяем поле ввода адреса
		Рез.wnd.Элементы.Шапка.selectCell(Рез.wnd.Элементы.Шапка.getRowIndex("shipping_address"), 1, false, true, true);
		// Имитируем щелчок мышью на этом поле - открывается форма ввода адреса
		Рез.wnd.Элементы.Шапка.cells().cell.firstChild.lastChild.click();
		// Можно изменять масштаб карты и перемещать маркер в нужную точку - адрес пересчитывается синхронно
	});