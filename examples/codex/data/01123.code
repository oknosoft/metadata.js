// Открываем форму первого попавшегося заказа
var Форма = Документы.ЗаказПокупателя.Найти().ФормаОбъекта(ОбластьКонтента, {Приклеить: Истина});
// Выделяем поле ввода адреса
Форма.Элементы.Шапка.selectCell(Форма.Элементы.Шапка.getRowIndex("shipping_address"), 1, false, true, true);
// Имитируем щелчок мышью на этом поле - открывается форма ввода адреса
Форма.Элементы.Шапка.cells().cell.firstChild.lastChild.click();
// Можно изменять масштаб карты и перемещать маркер в нужную точку - адрес пересчитывается синхронно